{
  "name": "former-fireball",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "clean": "rm -rf node_modules dist .astro .cache",
    "push-source": "git add . && git commit -m 'Update source' || echo 'Nothing to commit' && git push origin main",
    "deploy": "gh-pages -d dist",
    "full-deploy": "npm run clean && npm install && npm run build && npm run push-source && npm run deploy"
  },
  "devDependencies": {
    "astro": "^3.10.2",
    "gh-pages": "^5.1.0",
    "tailwindcss": "^4.2.1",
    "@tailwindcss/postcss": "^2.1.0",
    "postcss": "^8.4.36",
    "rss-parser": "^3.12.0"
  },
  "dependencies": {}
}